#!/bin/bash -e

echo "${TRAEFIK_PROVIDER_NAME}"
echo $(env)

find "${SNAP}/etc/traefik/provider/*.yaml"

#? https://www.baeldung.com/linux/envsubst-command
#? https://skofgar.ch/dev/2020/08/how-to-quickly-replace-environment-variables-in-a-file/
# envsubst <  # > ${SNAP}/traefik-providers/providers/${SNAP_NAME}.yaml > /etc/traefik/traefik.toml
